<div class="col-md-12">
  <div class="group">
    <%= link_to(" 编 辑 ", edit_group_path(@group), class:"btn btm-primary pull-right") %>
  </div>

  <h2><%= @group.title %></h2>
  <p>
    <%= @group.description %>
  </p>
</div>
